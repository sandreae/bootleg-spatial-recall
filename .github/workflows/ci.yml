name: ci

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v2

      - name: Build container 🏗
        run: docker-compose up -d
          
      - name: Wait for ready ...
        run: docker run --network container:bootleg-spatial-recall appropriate/curl -s --retry 10 --retry-connrefused http://localhost:3000/

      - name: Run linter 👀
        run: docker-compose exec -e 'NODE_ENV=test' test-server npm run lint

      - name: Run tests 🧪
        run: docker-compose exec -e 'NODE_ENV=test' test-server npm run test
